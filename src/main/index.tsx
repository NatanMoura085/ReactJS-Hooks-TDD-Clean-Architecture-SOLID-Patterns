import React from "react";
import  ReactDOM  from "react-dom";
import {Router} from "@/presentetion/components";
import "@/presentetion/styles/global-styles.scss"

ReactDOM.render(
    <Router/>,
    document.getElementById('main')
)